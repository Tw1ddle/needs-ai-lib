// This is solely for generating documentation for Needs AI using dox and HaxeUmlGen

import needs.ai.Action;
import needs.ai.ActionPickingStrategies;
import needs.ai.ActionScorePair;
import needs.ai.ActionScoringStrategies;
import needs.ai.ActionSet;
import needs.ai.Brain;
import needs.ai.Consideration;
import needs.ai.Reasoner;
import needs.ai.ReasonerActionPickingStrategies;
import needs.ai.WatchedBrain;
import needs.inputs.Input;
import needs.inputs.DistanceInput;
import needs.responses.Linear;
import needs.responses.Logistic;
import needs.responses.Logit;
import needs.responses.Normal;
import needs.responses.PiecewiseLinear;
import needs.responses.Polynomial;
import needs.responses.Response;
import needs.responses.Sine;
import needs.util.MathUtil;
import needs.util.Signal;

class Main {}